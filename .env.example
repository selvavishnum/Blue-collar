# ─── Frontend env (VITE_ prefix exposes to browser) ──────────────────────────

# Local dev: backend runs on :3001
VITE_API_URL=http://localhost:3001

# Render production: set this in Render → blue-collar-frontend → Environment
# VITE_API_URL=https://blue-collar-api.onrender.com
